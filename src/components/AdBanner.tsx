import { C } from '../styles/tokens';

interface AdBannerProps {
  /** ID del espacio publicitario opcional de Google AdSense */
  slot?: string;
  style?: React.CSSProperties;
}

/**
 * Componente de espacio publicitario optimizado para Google AdSense.
 * Diseñado con estética Cyberpunk para integrarse limpiamente en la app.
 */
/**
 * Componente publicitario desactivado.
 * Tras la transicion al modelo Freemium Premium / Licencia Ronin,
 * la app es 100% libre de anuncios y banners invasivos.
 */
export function AdBanner(_props: AdBannerProps) {
  return null;
}
